{
  "name": "@fireworks/typescript-plugin",
  "version": "0.0.1",
  "type": "module",
  "description": "A TypeScript plugin to support language features in AIML files",
  "license": "MIT",
  "main": "./dist/index.cjs",
  "exports": {
    "default": "./dist/index.cjs"
  },
  "files": ["dist"],
  "keywords": ["IntelliSense", "aiml", "typescript"],
  "scripts": {
    "prepack": "tsc --build --clean && tsc --build",
    "test-api": "node --test",
    "test": "npm pack && npm run test-api",
    "build": "tsc --build"
  },
  "dependencies": {
    "@fireworks/language-service": "0.0.1",
    "@volar/typescript": "~2.4.0",
    "load-plugin": "^6.0.0",
    "remark-frontmatter": "^5.0.0",
    "remark-gfm": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  },
  "sideEffects": false
}
